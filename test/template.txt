import test from 'tape-catch';

import name from '../src/path/to/module';

test('path/to/module/name', main => {
    main.test('├ overall behaviour', t => {
        t.test('├─ specific circumstances (ie. invalid input)', t => {

        });
        t.test('├─ other specific circumstances (ie. valid input)', t => {

        });
        t.test('├─ other specific circumstances (ie. some specific input)', t => {

        });
    });
    main.test('├ other behaviour', t => {
        t.test('├─ specific circumstances (ie. invalid input)', t => {

        });
        t.test('├─ other specific circumstances (ie. valid input)', t => {

        });
        t.test('├─ other specific circumstances (ie. some specific input)', t => {

        });
    });
});